/**
 * 簡易的なTypeScriptファイル
 * ts-nodeで実行するためのサンプル
 */

console.log("Hello from playwright-sse-mcp-server!");
console.log("ts-nodeを使って正常に実行されました。");

// 現在の日時を表示
const now = new Date();
console.log(`現在の日時: ${now.toLocaleString()}`);
